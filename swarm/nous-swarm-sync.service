[Unit]
Description=NOUS Swarm peer sync
After=nous-swarm.service

[Service]
Type=oneshot
User=nous
ExecStart=/srv/nous/swarm/.venv/bin/python3 -c "import asyncio, httpx; asyncio.run(httpx.AsyncClient(timeout=120).post('http://localhost:8020/swarm/sync'))"
EnvironmentFile=-/srv/nous/.env
